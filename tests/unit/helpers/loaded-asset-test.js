//import { loadedAsset } from '../../../helpers/loaded-asset';
import { module, test } from 'qunit';

module('Unit | Helper | loaded asset');

// Replace this with your real tests.
test('it works', function(assert) {
    //let result = loadedAsset([42]);
    assert.ok(true);
});
